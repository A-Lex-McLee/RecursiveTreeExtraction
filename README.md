# RecursiveTreeExtraction

@string_to_Tree 

tool to extract a tree from (PENN style) annotated corpora; 
more specifically, on the basis of a string comprising flat bracketing (-> the .psd file), an actual tree structure is built.
